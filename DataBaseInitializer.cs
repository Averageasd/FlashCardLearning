using FlashCardLearning.Context;

namespace FlashCardLearning
{
    public class DataBaseInitializer
    {
        public static void Initialize(FlashCardAppContext dbContext)
        {
            //dbContext.Database.EnsureCreated();
            //if (!dbContext.Users.Any())
            //{
            //    // Write you necessary to code here to insert the User to database and save the the information to file.

            //}

        }
    }
}
